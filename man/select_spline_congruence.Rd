% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{select_spline_congruence}
\alias{select_spline_congruence}
\title{Select a continuous congruence surface}
\usage{
select_spline_congruence(
  formula,
  data,
  center = c("pooled", "variablewise", "none"),
  scale = c("pooled", "none"),
  hinge_offset = 1,
  R = 1999,
  alpha = 0.05,
  min_arm_n = 30L,
  min_arm_prop = 0.1,
  max_fail = 0.1,
  seed = NULL,
  include_two_seam = TRUE
)
}
\arguments{
\item{formula, data}{Model formula and data accepted by
\code{\link[=fit_spline_congruence]{fit_spline_congruence()}}.}

\item{center, scale}{Congruence-preserving transformation options passed to
\code{\link[=prepare_congruence_data]{prepare_congruence_data()}}.}

\item{hinge_offset}{Working-scale offset used for fixed two-seam benchmarks.}

\item{R}{Number of null-bootstrap resamples for each structural comparison.}

\item{alpha}{Unadjusted decision threshold.}

\item{min_arm_n, min_arm_prop}{Minimum count and proportion required on each
side of the freely estimated seam.}

\item{max_fail}{Maximum acceptable failed-refit proportion for a bootstrap
structural test.}

\item{seed}{Optional integer seed. The LOC test uses \code{seed + 1}.}

\item{include_two_seam}{Whether to fit a two-seam sensitivity model.}
}
\value{
An object of class \code{spline_selection} containing \code{fits}, \code{tests},
\code{diagnostics}, \code{selected_model}, \code{selected_fit}, \code{status}, and \code{reason}.
}
\description{
Fit a linear plane, a one-seam spline fixed to the line of congruence, and a
freely located one-seam spline, then select among them using residual-
bootstrap likelihood-ratio tests. Absolute-difference and piecewise models
are retained as benchmarks, while an optional two-seam spline is retained as
a sensitivity analysis and is never selected as the focal model.
}
\details{
The seam-existence comparison is non-regular because the seam location is
unidentified under a linear surface. It therefore uses a null residual
bootstrap rather than the ordinary F or Wald reference distribution. The
fixed-versus-free seam comparison is performed only after the seam is
supported and both arms contain enough observations.
}
