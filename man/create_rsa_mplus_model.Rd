% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsa-mplus-workflow.R
\name{create_rsa_mplus_model}
\alias{create_rsa_mplus_model}
\alias{write_rsa_mplus_model}
\alias{run_rsa_mplus_model}
\alias{read_rsa_mplus_model}
\alias{rsa_mplus_workflow}
\title{Build, write, run, and read RSA models for Mplus}
\usage{
create_rsa_mplus_model(
  data,
  measurement,
  structural = NULL,
  roles = c(x = "X", y = "Y", outcome = "Z"),
  model_type = c("latent", "si_lms"),
  reliability = NULL,
  constraints = c("surface", "principal"),
  blocks = list(),
  model_extra = NULL,
  constraint_extra = NULL,
  usevariables = NULL,
  quiet = TRUE
)

write_rsa_mplus_model(
  x,
  modelout,
  dataout = NULL,
  overwrite = FALSE,
  quiet = TRUE
)

run_rsa_mplus_model(
  x,
  Mplus_command = NULL,
  replace_outfile = c("always", "never", "modifiedDate"),
  quiet = TRUE
)

read_rsa_mplus_model(target, output = NULL, quiet = TRUE)

rsa_mplus_workflow(
  data,
  measurement,
  structural = NULL,
  roles = c(x = "X", y = "Y", outcome = "Z"),
  model_type = c("latent", "si_lms"),
  reliability = NULL,
  constraints = c("surface", "principal"),
  blocks = list(),
  model_extra = NULL,
  constraint_extra = NULL,
  usevariables = NULL,
  modelout,
  dataout = NULL,
  overwrite = FALSE,
  run = FALSE,
  Mplus_command = NULL,
  quiet = TRUE
)
}
\arguments{
\item{data}{A data frame containing all variables exported to Mplus.}

\item{measurement}{A named list mapping latent variables to indicators, or a
character vector of lavaan/tidySEM measurement statements.}

\item{structural}{Optional structural model. Supply a named list mapping
outcomes to predictors, or a character vector of lavaan/tidySEM paths.
The core X and Y paths to the RSA outcome are added when absent.}

\item{roles}{Named character vector with elements \code{x}, \code{y}, and \code{outcome}
identifying the latent variables used in the RSA model.}

\item{model_type}{Either \code{"latent"} for multi-indicator factors or
\code{"si_lms"} for reliability-corrected single-indicator factors.}

\item{reliability}{For \code{model_type = "si_lms"}, a named numeric vector of
reliability estimates for the three latent variables in \code{roles}.}

\item{constraints}{Which built-in constraints to include. \code{"surface"}
produces \code{cs}, \code{cc}, \code{is}, \code{ic}, and \code{a5}; \code{"principal"} additionally
produces the stationary point and principal axes.}

\item{blocks}{Named list of Mplus input blocks. Values replace defaults for
the corresponding block. \code{MODEL} and \code{MODELCONSTRAINT} are reserved; use
\code{model_extra} and \code{constraint_extra} to extend them. The \code{VARIABLE} block
may contain additional directives, but \code{USEVARIABLES} is generated from
\code{usevariables} and must not be supplied through \code{blocks}.}

\item{model_extra}{Additional raw statements appended to the Mplus \code{MODEL}
block.}

\item{constraint_extra}{Additional raw statements appended to the Mplus
\verb{MODEL CONSTRAINT} block.}

\item{usevariables}{Character vector of data columns to export and include
in the Mplus \code{USEVARIABLES} statement. Defaults to all columns in \code{data}.}

\item{quiet}{If \code{TRUE}, suppress status messages from MplusAutomation.}

\item{x}{An \code{rsa_mplus_workflow} object.}

\item{modelout}{Path to the Mplus \code{.inp} file to create.}

\item{dataout}{Path to the Mplus data file. By default, it uses the same
stem as \code{modelout} with a \code{.dat} extension.}

\item{overwrite}{If \code{TRUE}, replace existing input and data files.}

\item{Mplus_command}{Optional path or command used to invoke Mplus.}

\item{replace_outfile}{Passed to \code{\link[MplusAutomation:runModels]{MplusAutomation::runModels()}}.}

\item{target}{An \code{rsa_mplus_workflow}, Mplus \code{.inp} or \code{.out} path, an
\code{mplus.model}, or an \code{mplusObject} containing parsed results.}

\item{output}{Optional Mplus \code{.out} path to read into an existing workflow.
This is useful for attaching a cached or relocated output while retaining
the workflow's role metadata. It can only be used when \code{target} is an
\code{rsa_mplus_workflow}.}

\item{run}{If \code{TRUE}, run and read the generated Mplus model after writing
its files. The default only writes reproducible input and data files.}
}
\value{
An object of class \code{rsa_mplus_workflow}. The object is updated and
returned by the write, run, and read functions.
}
\description{
These functions provide a composable workflow for response surface models
estimated in Mplus. \code{\link[=create_rsa_mplus_model]{create_rsa_mplus_model()}} uses tidySEM for ordinary
measurement and structural paths, then adds the \code{XWITH} interactions and
response-surface constraints required for latent polynomial models.
}
\examples{
\dontrun{
latent_model <- create_rsa_mplus_model(
  data = dat,
  measurement = list(
    X = c("x1", "x2", "x3"),
    Y = c("y1", "y2", "y3"),
    Z = c("z1", "z2", "z3")
  ),
  structural = list(Z = c("X", "Y"))
)
}
}
