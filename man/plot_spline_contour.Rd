% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{plot_spline_contour}
\alias{plot_spline_contour}
\title{Plot a fitted congruence spline surface as a 2D contour map}
\usage{
plot_spline_contour(
  fit,
  grid_size = 80,
  xlim = NULL,
  ylim = NULL,
  equal_limits = TRUE,
  palette = c("#a50026", "#d73027", "#f46d43", "#fdae61", "#fee08b", "#ffffbf",
    "#d9ef8b", "#a6d96a", "#66bd63", "#1a9850", "#006837"),
  bins = 12,
  show_seams = TRUE,
  show_fit_line = TRUE,
  seam_col = "#2166ac",
  seam_lwd = 1.1,
  fit_line_col = "grey25",
  fit_line_lty = "dashed",
  xlab = "X (centered)",
  ylab = "Y (centered)",
  fill_lab = "Outcome",
  main = "Congruence spline surface"
)
}
\arguments{
\item{fit}{A \code{congruence_spline} object.}

\item{grid_size}{Integer number of grid points per axis.}

\item{xlim, ylim}{Optional axis limits. If either is \code{NULL} and
\code{equal_limits = TRUE}, both axes use one shared symmetric range.}

\item{equal_limits}{Logical. If \code{TRUE}, use one shared symmetric range for
both axes.}

\item{palette}{Either a vector of colors to interpolate or the name of a
\code{\link[grDevices:hcl.colors]{grDevices::hcl.colors()}} palette for the fill gradient.}

\item{bins}{Number of contour bins.}

\item{show_seams}{Logical. If \code{TRUE}, overlay the estimated seam line(s).}

\item{show_fit_line}{Logical. If \code{TRUE} (default), overlay the theoretical
line of congruence \code{X = Y}.}

\item{seam_col, seam_lwd}{Seam line color and width.}

\item{fit_line_col, fit_line_lty}{Line style for \code{X = Y}.}

\item{xlab, ylab, fill_lab, main}{Axis, legend, and title labels.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw the fitted spline surface as a filled \code{ggplot2} heatmap with contour
lines, the estimated seam line(s), and (optionally) the line of congruence.
A 2D view is often clearer than \code{\link[=plot_spline_surface]{plot_spline_surface()}} for reading where the
seams fall. Requires the \code{ggplot2} package.
}
\examples{
\dontrun{
plot_spline_contour(fit)
}

}
