% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify-yao-ma.R
\name{classify_yao_ma}
\alias{classify_yao_ma}
\title{Classify a response surface using the Yao-Ma typology}
\usage{
classify_yao_ma(x, valence, equivalence = NULL, level = 0.95)
}
\arguments{
\item{x}{An \code{\link[=RSA_mplus]{RSA_mplus()}} result, an \code{rsa_mplus_workflow}, an
\code{mplus.model}, an \code{mplusObject}, or a data frame containing response-surface
parameters. A data frame must have columns \code{parameter}, \code{estimate},
\code{conf.low}, and \code{conf.high}.}

\item{valence}{Whether higher (\code{"positive"}) or lower (\code{"negative"}) values
of the outcome are desirable. This argument is required because it selects
the relevant principal axis.}

\item{equivalence}{Optional named numeric vector of practical-equivalence
margins. Supply one nonnegative margin for each of \code{CS}, \code{CC}, \code{IS}, \code{IC},
\code{axis_intercept}, and \code{axis_slope}. When omitted, confidence intervals that
include the null are treated as absence of an effect. When supplied, each
condition is classified as equivalent, different, or indeterminate.}

\item{level}{Confidence level used when extracting intervals from Mplus.
Intervals already present in a parameter data frame are used unchanged.}
}
\value{
An object of class \code{yao_ma_classification}. It contains a one-row
\code{summary}, the normalized \code{parameters}, and a condition-level \code{decisions}
data frame.
}
\description{
Classify a response surface using the Yao-Ma typology
}
\details{
The function uses \code{CS}, \code{CC}, \code{IS}, and \code{IC}, corresponding to the usual
RSA parameters \code{a1} through \code{a4}. For positively valenced outcomes it uses
\code{P10} and \code{P11}; for negatively valenced outcomes it uses \code{P20} and \code{P21}.

Principal-axis parameters are nonlinear functions of the polynomial
coefficients. Mplus confidence intervals for these \verb{MODEL CONSTRAINT}
parameters use its delta-method standard errors. A missing or degenerate
principal axis therefore produces a partial result rather than an invented
Yao-Ma subtype.
}
\examples{
params <- data.frame(
  parameter = c("CS", "CC", "IS", "IC", "P10", "P11"),
  estimate = c(0.4, 0.01, 0.02, -0.5, 0.03, 1.02),
  conf.low = c(0.2, -0.1, -0.1, -0.7, -0.1, 0.9),
  conf.high = c(0.6, 0.1, 0.1, -0.3, 0.1, 1.1)
)
classify_yao_ma(params, valence = "positive")
}
