% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{fit_spline_congruence}
\alias{fit_spline_congruence}
\title{Fit an Edwards-Parry congruence spline surface}
\usage{
fit_spline_congruence(
  data,
  wanted,
  actual,
  outcome,
  n_seams = c(1L, 2L),
  center = TRUE,
  starts = NULL,
  multistart = TRUE,
  control = list(maxit = 10000),
  prefer_nlslm = TRUE
)
}
\arguments{
\item{data}{A data frame containing the component and outcome variables.}

\item{wanted, actual}{Column names, quoted or unquoted, for the two component
variables. \code{wanted} is modeled as \code{x}; \code{actual} is modeled as \code{y}.}

\item{outcome}{Column name, quoted or unquoted, for the response variable.}

\item{n_seams}{Number of seams to estimate. Supported values are \code{1} and
\code{2}.}

\item{center}{Logical. If \code{TRUE}, subtract each component's mean before
fitting. Use \code{FALSE} for pre-centered columns.}

\item{starts}{Optional named numeric vector or list of named numeric vectors
with starting values. One-seam names are \code{b0}, \code{b1}, \code{b2}, \code{b3}, \code{c0},
and \code{c1}. Two-seam names are \code{b0}, \code{b1}, \code{b2}, \code{b3}, \code{b4}, \code{c10}, \code{c11},
\code{c20}, and \code{c21}.}

\item{multistart}{Logical. If \code{TRUE}, fit from Stata-style constrained and
unconstrained starts plus small jittered variants and keep the lowest-RSS
converged solution.}

\item{control}{List of solver control options. \code{maxit} is accepted for
convenience and translated to \code{minpack.lm}'s \code{maxiter}.}

\item{prefer_nlslm}{Logical. If \code{TRUE} and \code{minpack.lm} is installed, use
\code{minpack.lm::nlsLM()}. Otherwise use a base-R \code{optim()} fallback.}
}
\value{
A \code{congruence_spline} object, a list containing coefficients,
covariance matrix, fitted values, residuals, RSS, R-squared, residual
degrees of freedom, solver metadata, and all start attempts.
}
\description{
Estimate one- or two-seam spline regression surfaces for congruence
hypotheses using nonlinear least squares. The one-seam model matches
Equation 39 in Edwards and Parry (2018); the two-seam model extends it with
a second estimated seam term.
}
\examples{
\dontrun{
fit <- fit_spline_congruence(dat, wanted, actual, satisfaction, n_seams = 1)
coef(fit)
surface_features(fit)
}

}
