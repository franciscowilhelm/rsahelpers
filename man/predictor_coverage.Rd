% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predictor-coverage.R
\name{predictor_coverage}
\alias{predictor_coverage}
\title{Diagnose joint predictor coverage for response-surface analysis}
\usage{
predictor_coverage(
  data,
  x,
  y,
  breaks = 10L,
  discrepancy = NULL,
  min_count = 5L
)
}
\arguments{
\item{data}{A data frame containing the two commensurate predictors.}

\item{x, y}{Predictor columns, supplied as unquoted names or strings.}

\item{breaks}{Either the number of shared equal-width bins (default \code{10}) or
a numeric vector of common breakpoints for both predictors.}

\item{discrepancy}{Optional positive cutoffs at which to summarize the
proportions of observations with \code{X - Y} or \code{Y - X} at least that large.}

\item{min_count}{Minimum cell count considered adequately covered.}
}
\value{
An object of class \code{predictor_coverage}. It contains \code{cells}, a
one-row \code{summary}, an optional \code{discrepancy} table, the shared \code{breaks},
and plotting metadata.
}
\description{
Diagnose joint predictor coverage for response-surface analysis
}
\details{
Shared breaks preserve the geometry of the line \code{X = Y}. This is especially
useful for scale scores derived from Likert items: using every observed
decimal value creates an unnecessarily sparse grid, while separate quantile
bins distort congruence and discrepancy regions.
}
\examples{
scores <- data.frame(
  self = c(1.0, 1.5, 2.2, 3.1, 3.8, 4.4, 4.8),
  other = c(1.2, 1.6, 2.7, 2.9, 3.5, 4.1, 4.7)
)
coverage <- predictor_coverage(
  scores,
  self,
  other,
  breaks = 4,
  discrepancy = c(0.5, 1)
)
coverage$summary
}
