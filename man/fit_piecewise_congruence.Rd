% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{fit_piecewise_congruence}
\alias{fit_piecewise_congruence}
\title{Fit OLS comparison models for congruence analyses}
\usage{
fit_piecewise_congruence(
  formula,
  data,
  center = c("pooled", "variablewise", "none"),
  scale = c("pooled", "none"),
  hinge_offset = 1,
  n_seams = c(1L, 2L),
  constrained = TRUE
)
}
\arguments{
\item{formula}{A two-sided formula of the form \code{z ~ x * y}. The response is
mapped to Z, the first predictor to X, and the second predictor to Y. The
\code{*} declares the two predictor roles rather than an ordinary interaction.}

\item{data}{A data frame containing the Z, X, and Y variables.}

\item{center, scale}{Centering and scaling of the component variables, passed
to \code{\link[=prepare_congruence_data]{prepare_congruence_data()}}. Defaults to pooled centering and pooled
scaling.}

\item{hinge_offset}{Numeric half-width, in working-scale units, for the
fixed two-seam OLS seam lines \verb{Y = X ± hinge_offset}. See
\code{\link[=prepare_congruence_data]{prepare_congruence_data()}}.}

\item{n_seams}{Number of seams to include in comparison models. If \code{2}, add
the fixed two-seam OLS model using seam lines \code{Y = X + hinge_offset} and
\code{Y = X - hinge_offset}.}

\item{constrained}{Logical. If \code{FALSE}, omit the constrained one-seam OLS
model from the returned list.}
}
\value{
A named list of \code{lm} objects with class \code{congruence_piecewise}.
}
\description{
Fit the absolute-difference, linear, one-break piecewise, constrained
one-seam piecewise, and optionally fixed two-seam OLS models used as
comparisons and starting-value sources for spline regression.
}
\examples{
\dontrun{
fits <- fit_piecewise_congruence(satisfaction ~ x * y, dat)
tidy_piecewise_summary(fits)
}

}
