% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{fit_piecewise_congruence}
\alias{fit_piecewise_congruence}
\title{Fit OLS comparison models for congruence analyses}
\usage{
fit_piecewise_congruence(
  data,
  wanted,
  actual,
  outcome,
  center = TRUE,
  n_seams = c(1L, 2L),
  constrained = TRUE
)
}
\arguments{
\item{data}{A data frame containing the component and outcome variables.}

\item{wanted, actual}{Column names, quoted or unquoted, for the two component
variables. \code{wanted} is modeled as \code{x}; \code{actual} is modeled as \code{y}.}

\item{outcome}{Column name, quoted or unquoted, for the response variable.}

\item{center}{Logical. If \code{TRUE}, subtract each component's mean before
constructing model terms.}

\item{n_seams}{Number of seams to include in comparison models. If \code{2}, add
the fixed two-seam OLS model using seam lines \code{Y = X + 1} and \code{Y = X - 1}.}

\item{constrained}{Logical. If \code{FALSE}, omit the constrained one-seam OLS
model from the returned list.}
}
\value{
A named list of \code{lm} objects with class \code{congruence_piecewise}.
}
\description{
Fit the absolute-difference, linear, one-break piecewise, constrained
one-seam piecewise, and optionally fixed two-seam OLS models used as
comparisons and starting-value sources for spline regression.
}
\examples{
\dontrun{
fits <- fit_piecewise_congruence(dat, wanted, actual, satisfaction)
tidy_lm_summary(fits)
}

}
