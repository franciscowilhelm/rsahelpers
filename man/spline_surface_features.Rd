% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spline-regression.R
\name{spline_surface_features}
\alias{spline_surface_features}
\title{Compute derived surface features for a fitted spline model}
\usage{
spline_surface_features(fit, lines = c(-1, 0, 1))
}
\arguments{
\item{fit}{A \code{congruence_spline} object.}

\item{lines}{Numeric vector giving the \code{k} values where lines of interest
cross \code{Y = X} at \verb{(k, k)}. For one-seam models, each line is
\verb{Y = 2k - X}.}
}
\value{
A named numeric vector of derived quantities. One-seam models return
the full Edwards-Parry set currently implemented. Two-seam models return
the seam shifts along \code{Y = -X}, the section slopes in \code{x} and \code{y} for each
active-hinge region (base, seam1-only, seam2-only, both), and a crossing
diagnostic: \code{seams_cross} (1 if the seams cross within the observed \code{x}
range, else 0), \code{crossing_x}, and \code{n_sections} (3 if the seams cross inside
the data, else 4).
}
\description{
Derive interpretable features from the fitted surface, including side
slopes, along-seam slopes, symmetry expressions, and seam shifts along lines
parallel to \code{Y = -X}.
}
\examples{
\dontrun{
spline_surface_features(fit, lines = c(-1, 0, 1))
}

}
